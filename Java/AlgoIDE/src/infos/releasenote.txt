**** v1.1.0 ****
- Light look and feel
- toJSon() dynamic method that marshall expression to json text format
- fromJSon() method that unmarshall json text to expression
- object.fromJSon() dynamic method that merge object with json values
- string \" escape term
- 3d spline and starbattle but correction
- "paste" syntax correction

**** v1.0.1 ****
- Case insensitive language
- Remove default borders

**** v1.0.0 ****
- New icons
- New tools (undo, redo, cut, copy, past)
- number operations:
	* NUMBER.limit
- algo RGB color support:
        * algo.setRGB()
        * algo.setBgRGB()
- rotate images like turtle
- algo bitmap support: algo.stamp.load(filename)

**** v0.3.1 (rc2) ****
- fix clone parameter evaluation bug
- array set operations:
	* ARRAY.containsAll
	* ARRAY.containsAny
	* ARRAY.union
	* ARRAY.intersection
	* ARRAY.complement
	* ARRAY.difference
	* ARRAY.product

**** v0.3.0 (rc1) ****
- fix tail call bug
- fix events scope bug
- all algoid variables onChanged event!
- rename al.color.CYANN by al.color.CYAN (idem for DARK_CYANN)
- move util.eval to eval (root scope)
- new function exists to test if variable exists
- new methods util.toClipboard and util.fromClipboard

**** v0.2.13 (beta) ****
- lambda expression (syntactic sugar)
- fix text.inputNumber string result issue (cast to number)
- [all].ifNotBreak() and [all].ifNullBreak()
- algo.autoClear for game render loop (no flick anymore)

**** v0.2.12 (beta) ****
- correct other locales bug (thanks to Nikita)
- add nan, infinity keywords
- add NUMBER.isNan, NUMBER.isInfinite methods
- rename NUMBER.isDecimal to NUMBER.isInteger
- rename NUMBER.toDecimal to NUMBER.toInteger
- modify ARRAY.create (to, function)
- variable value tips when running step by step
- object.toString

**** v0.2.11 (beta) ****
- correct auto indentation strange behaviors

**** v0.2.10 (beta) ****
- correct plugins load bug at startup
- correct analytics

**** v0.2.9 (beta) ****
- algo.color constants (RED, BLACK, GREEN etc.)
- improve brace auto completion (smart closing)
- automatic save on temporary file (to prevent work loss)
- auto indentation tool (new menu bar button)
- manage tab during typing
- remove infinite loops detection (must be implemented again as semantic analysis)
- tail recursion optimization (use trampoline algorithm)

**** v0.2.8 (beta) ****
- array index scope correction
- infinite loops detection
- multiline tab

**** v0.2.7 (beta) ****
- aop assignation correction
- al.order.reverse correction
- al.allLocalObjects, get all the objects names in the current scope

**** v0.2.6 (beta) ****
- add graphic calculator 2d.al example
- correct Issue#3 bug (returned function scope tree)
- add print function
- add [all].ifNull magic method to all types
- If is now an expression (set s = if (a==0) "zero" else "other")
- al.allObjects get all the objects names in the root scope

**** v0.2.5 (beta) ****
- improve auto-completion flexibility.
- multilingual : french translation
- [all].toString() magic method
- correct program error crash the ide with algo.onMove event (thanks to Clément ;-)

**** v0.2.4 (beta) ****
- algo.getX() / algo.getY / algo.getAngle.
- OBJECT.clone, automatic assignment.
- plugins ready.
- fix algo.removeLast and algo.removeFirst if algo's shapes are empty.
- auto completion (hit ctrl + space) and help for control structures.
- algo.onKey to manage keyboard events
- algo.onWheel to manage mouse wheel events
- algo.onTouch / algo.onTap / algo.onUp compatibility
- remove algo.import
